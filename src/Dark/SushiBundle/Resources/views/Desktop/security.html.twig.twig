<html>
<head>
   <title>Login</title>
   <link rel='stylesheet' type='text/css' href='{{asset("css/helper.css")}}' media="screen"/>
   <link rel='stylesheet' type='text/css' href='{{asset("css/scroll.css")}}' media="screen"/>
   <link rel="stylesheet" type="text/css" href="{{asset("css/picBox.css")}}" media="screen">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.11.1/jquery.min.js" type="text/javascript"></script>
   <script src="{{asset('js/showAndHide.js')}}"></script>
</head>

    <body>


{% if error %}
    <div>{{ error.messageKey|trans(error.messageData, 'security') }}</div>
{% endif %}

<form action="{{ path('login') }}" method="post">
    <label for="username">Username:</label>
    <input type="text" id="username" name="_username" value="{{ last_username }}" />

    <label for="password">Password:</label>
    <input type="password" id="password" name="_password" />

    {#
        If you want to control the URL the user
        is redirected to on success (more details below)
        <input type="hidden" name="_target_path" value="/account" />
    #}

    <button type="submit">login</button>
</form>

    </body>
</html>